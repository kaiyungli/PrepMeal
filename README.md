PrepMeal Test + Refactor Bundle

Steps:

1. Copy src/lib files into your repo.
2. Copy tests folder into repo root.
3. Install Vitest:
   npm install vitest --save-dev

4. Add script to package.json:
   "test": "vitest"

5. Run:
   npm run test