'use client';

/**
 * AdSense placeholder - script now loaded via _app.js
 * This component is kept for backward compatibility
 */

export default function AdSenseLoader({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
