/**
 * Generate feature — public surface.
 *
 * The 今日 route imports `GenerateScreen` from here. Everything else (the
 * vendored planner engine, the lean fetch, hooks, pure helpers) is internal.
 */
export { GenerateScreen } from './components/GenerateScreen.tsx';
